{
	"name": "react-cra-mf",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"start": "lerna run start --stream --parallel",
		"preview": "lerna run preview --stream --parallel",
		"issue": "lerna run start --scope=host-app --stream & lerna run preview --scope=remote-app --stream"
	},
	"devDependencies": {
		"lerna": "^8.1.8"
	}
}
